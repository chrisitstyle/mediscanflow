package com.chrisitstyle.mediscanflow.medicalplatform.audit;

/**
 * Marker interface for typed audit event metadata.
 *
 * <p>Implementations represent structured metadata that can be serialized
 * and stored together with an audit event.</p>
 */
public interface AuditMetadata {
}
